# Design reference

Drop UI reference screenshots here (for example `hero.png`, `colors.png`) when you want the next pass to align Tailwind tokens (`frontend/tailwind.config.js`) and base styles with a visual target.

Tokens and component classes live in `frontend/tailwind.config.js` and `frontend/src/styles/index.css` (`.section`, `.card`, `.btn-primary`, `.btn-secondary`). Drop screenshots here to tune greens, creams, or shadows against a visual reference.
